import { Router } from "express";

const router = Router();

router.get("/payroll/:id", async (req, res) => {
    // Queries user
    // Gets incomes, outcomes, salaries
    // Sums them up
    // Returns payroll
});

